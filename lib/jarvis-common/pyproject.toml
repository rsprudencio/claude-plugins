[project]
name = "jarvis-common"
version = "1.2.0"
description = "Shared configuration, path resolution, and namespace utilities for Jarvis MCP servers"
requires-python = ">=3.11"
dependencies = []  # stdlib only

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["jarvis_common"]
